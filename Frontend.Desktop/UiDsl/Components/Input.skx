<box id="{id}" x="{x || '0'}" y="{y || '0'}" width="{width || '200'}" height="{height || '40'}" 
  fill="{backgroundColor || '#FFFFFF'}" 
  border="{borderColor || '#CBD5E1'}" 
  borderWidth="{borderWidth || '1'}" 
  onClick="{onClick || 'EditField:' + id}">
  <text x="12" y="12" text="{value || placeholder || ''}" fontSize="{fontSize || '16'}" textColor="{value ? (textColor || '#334155') : '#94A3B8'}"></text>
</box> 